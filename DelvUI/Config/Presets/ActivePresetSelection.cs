namespace DelvUI.Config.Presets
{
    public enum ActivePresetSelection
    {
        Custom = -1,
        Minimal = 0,
        MmoModern = 1,
        RaidFocused = 2,
        ActionCombat = 3
    }
}
